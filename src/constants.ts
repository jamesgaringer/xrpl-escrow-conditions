/** Default XRPL testnet JSON-RPC / WebSocket endpoint. */
export const XRPL_TESTNET_WSS = 'wss://s.altnet.rippletest.net/' as const

/** Unix timestamp (seconds) for 2000-01-01T00:00:00Z — XRPL "Ripple epoch" offset. */
export const RIPPLE_EPOCH_UNIX_OFFSET = 946684800 as const
